x=4.5
class(x) #看出其type

y="dog"
class(y)

r=c(1,2,3,4) #向量：將所有同一種型態連結在一起 語法：c()
class(r)

s=c("dog","cat",)
class(s)

a=c(1:10) #1~10
b=c(-2:5) #-2~5
a #兩項量可相加減乘除
a[1] #取出向量中第一個位置的數值

a[c(1,3,5)] #取1,3,5位置的值
a[c(1,2,3)]


